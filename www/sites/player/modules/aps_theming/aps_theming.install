<?php

/**
 * @file
 * Install, update, and uninstall functions for the aps_theming module.
 */

/**
 * Implements hook_install().
 */
function aps_theming_install() {
}

/**
 * Implements hook_field_schema().
 */
function aps_theming_field_schema($field) {
  switch ($field['type']) {
    case 'delta_type':
      $columns = array(
        'theme' => array(
          'type' => 'varchar',
          'length' => '255',
          'not null' => TRUE,
          'description' => 'Generated by Field Type Generator.',
        ),
      );
    break;
  }
  return array(
    'columns' => $columns,
  );
}

/**
 * Implements hook_uninstall().
 */
function aps_theming_uninstall() {
}
